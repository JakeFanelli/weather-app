import React from "react";
import { Navbar, NavbarBrand } from "reactstrap";

const MenuBar = props => (
  <Navbar className="bg-black">
    <NavbarBrand>Jake's Weather App</NavbarBrand>
  </Navbar>
);

export default MenuBar;
